# server settings
server.port=8080
management.port=7003

# datasource settings
spring.datasource.url=jdbc:mysql://localhost:3306/test
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.maxActive=20
spring.datasource.initialSize=1
spring.datasource.minIdle=1
spring.datasource.validationQuery=SELECT 1

# mybatis settings
mybatis.model==com.gitile.api.model
mybatis.mapper=com.gitile.api.mapper
mybatis.xml=classpath:mapper/*.xml

# LOGGING
logging.path=/var/logs
logging.file=/var/logs/api.log
logging.level.com.gitile=DEBUG

# /info endpoint
info.app.name=Spring Boot Example
info.app.version=1.0.0

# other settings
spring.main.show-banner=false